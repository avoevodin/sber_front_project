/* eslint-disable default-param-last */
const postsReducer = (store = [], action) => {
  switch (action.type) {
    case 'add_post':

      return store

    default:
      return store
  }
}

export default postsReducer
