// TODO
const options = {
  hour: '2-digit',
  minute: '2-digit',
  second: '2-digit',
}

const LSPostsKey = 'posts'
const LSCommentsKey = 'comments'

module.exports = {
  options,
  LSPostsKey,
  LSCommentsKey,
}
